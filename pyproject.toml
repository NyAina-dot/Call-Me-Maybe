[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Function calling with constrained decoding"
requires-python = ">=3.10"
dependencies = [
    "llm-sdk",
    "numpy>=2.0",
    "pydantic>=2.0",
    "flake8>=7.0",
    "mypy>=1.0",
    "poetry>=2.0",
]

[tool.uv.sources]
llm-sdk = { path = "llm_sdk" }